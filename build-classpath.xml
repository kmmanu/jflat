<project name="jflat-classpath" default="compile" basedir=".">

	<property file="build-code.properties" />

	<path id="class.path">
		<fileset dir="${lib.dir}">
			<include name="**.jar" />
		</fileset>
	</path>

	<path id="run.class.path">
		<path refid="class.path" />
		<dirset dir="${build.classes}" />
	</path>
	
</project>